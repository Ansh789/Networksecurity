### this is my network security project

Usage
-
- Run directly without installing:

```
python main.py list
python main.py run push_data.py
python main.py check
```

- Install editable and use the `networksecurity` CLI:

```
pip install -e .
networksecurity list
networksecurity run push_data.py
```
